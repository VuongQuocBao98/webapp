const styles = (theme) => ({
  slide: {
    maxHeight: "400px",
    minHeight: "400px",
    width: "100%",
  },
  imageContent: {
    maxHeight: "100px",
    minHeight: "100px",
    width: "100%",
  },
});

export default styles;
